<%= form_with(model: post, local: true) do |form| %>
  <% if post.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(post.errors.count, "error") %> prohibited this post from being saved:</h2>

      <ul>
      <% post.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :titulo %>
    <%= form.text_field :titulo, placeholder: "Insira o título" %>
  </div>

  <div class="field">
    <%= form.label :conteudo %>
    <%= form.text_area :conteudo, placeholder: "Escreva aqui..." %>
  </div>

  <div class="field">
    <%= form.label :autor %>
    <%= form.text_field :autor , placeholder: "Insira seu email" %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
